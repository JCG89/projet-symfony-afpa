<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <div class="container-fluid">
    <a class="navbar-brand"
      style="color:skyblue;"
      href="{{ path('app_home') }}">
      <img src="{{ asset('/images/logo/resize2.png') }}" class="logo" />Accueil
    </a>
    <button class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link"
              style="color:skyblue;"
              href="{{ path('app_pins_create') }}">
              Nouveau Monument
            </a>
          </li>

          <li class="nav-item">
            <a class="nav-link"
              style="color:skyblue;"
              href="{{ path('app_logout') }}">
              Deconnexion
            </a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link"
              style="color:skyblue"
              href="{{ path('app_register') }}">
              Inscription
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link mr-5"
              style="color:skyblue"
              href="{{ path('app_login') }}">
              Connexion
            </a>
          </li>
          <form class="form-inline">
            <input class="form-control mr-sm-2"
              type="text"
              placeholder="Rechercher" />
            <button class="btn btn-secondary my-2 my-sm-0 fontSize"
              type="submit"
              style="background-color:skyblue;">
              Rechercher
            </button>
          </form>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
